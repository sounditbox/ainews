x-app: &app
  build: .
  env_file: .env
  environment:
    CELERY_BROKER_URL: redis://redis:6379/0
    CELERY_RESULT_BACKEND: redis://redis:6379/1
    DATABASE_URL: postgresql+psycopg://postgres:postgres@postgres:5432/postgres
  depends_on:
    postgres:
      condition: service_healthy


services:
  postgres:
    image: postgres:17-alpine
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 3s
      timeout: 2s
      retries: 5

  redis:
    image: redis:7.4-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 2s
      retries: 5

# uncomment for non-local development
#  backend:
#    <<: *app
#    command: uv run -m uvicorn app.main:app --host 0.0.0.0 --port 8000
#    ports:
#      - "8000:8000"

  celery-worker:
    <<: *app
    command: uv run -m celery -A celery_app worker -l info

  celery-beat:
    <<: *app
    command: uv run -m celery -A celery_app beat -l info


volumes:
  postgres_data:
  redis_data:
